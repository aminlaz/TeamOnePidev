{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelecategorie</th>
                <td>{{ categorie.libelecategorie }}</td>
            </tr>
            <tr>
                <th>Nbrabo</th>
                <td>{{ categorie.nbrabo }}</td>
            </tr>
            <tr>
                <th>Nbrvu</th>
                <td>{{ categorie.nbrvu }}</td>
            </tr>
            <tr>
                <th>Photocategorie</th>
                <td>{{ categorie.photocategorie }}</td>
            </tr>
            <tr>
                <th>Desccategorie</th>
                <td>{{ categorie.desccategorie }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorie_edit', { 'id': categorie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
